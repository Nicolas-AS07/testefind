# Arquivo de exemplo de variáveis de ambiente para Vite
# Nenhuma variável é obrigatória no estado atual do projeto.
# Caso futuramente você adicione integrações (ex.: APIs), prefira prefixar como VITE_*
# Exemplo (descomentando e ajustando conforme necessário):
# VITE_API_BASE_URL="http://localhost:3000"
# VITE_FEATURE_FLAG_EXPERIMENTAL="false"

# Supabase (preencha com os dados do seu projeto)
# Copie este arquivo para .env e substitua pelos valores reais do Supabase
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=SUA_CHAVE_ANON
